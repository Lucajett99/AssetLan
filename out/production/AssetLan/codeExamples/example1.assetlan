 asset x ;
     void f(int n)[asset u, asset v]{
        print u; print v;
        if (n == 0) {u -o x ;}
	else { u -o x ; v -o x ; }
     }
     void main()[asset a, asset b]{
	f(2)[a,b] ;	// semantica di f()[a,a] ?
	print a;
	print b;
	transfer x ;
     }
     main()[1,2] ;	// il contratto e` liquido