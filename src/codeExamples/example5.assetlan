asset x;

bool g()[asset a] {
    transfer a;
    return true;
}

int f()[asset u, asset v, asset w]{
    if(g()[v]) {
        f()[x,w,u];
        g()[u];
        g()[v];
        g()[w];
    }
    else {
        f()[x,w,u];
        g()[u];
        g()[v];
        g()[w];
    }
    return 1;
}

void main()[asset a, asset b, asset c]{
   f()[a,b,c] ;
   transfer x ;
}
main()[1,2,3] ;