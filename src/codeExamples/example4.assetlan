asset x ;
bool g()[]{
    transfer x;
    return true;
}
void f(int n)[asset u, asset v]{
    if (n == 0){ u -o x ;}
else { u -o x ; v -o x ; }
}
void main()[asset a]{
    f(1)[a,a] ;	// semantica di f()[a,a] ?
    if(g()[]){print 1;}
}
main()[1] ;	// il contratto e` liquido
